export interface UserType {
  email: string;
  password: string;
  nickname: string;
  userid: number;
}

export const todos = ['다함', '님이', '다함'];

export const users: UserType[] = [];

export const data = [];
